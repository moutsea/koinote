# 更新履歴

Koinote の利用者に関係する主な変更を記録します。

## [Unreleased]

### Added

- 会員向け AI 最適化を追加しました。タイトルと本文の提案を Git Diff 形式で表示し、個別または一括で適用・無視できます。タイトルは 0–100 点で評価し、60 点未満では 2–3 件の候補を提示します。内容改善と構成・レイアウトを分離し、Markdown AST を使って階層、読みやすさ、強調、リズム、モジュール、モバイル表示を安全に評価・修正します。
- 終身会員に 1,000 credits を付与します。内蔵モデルは実使用 2,000 token ごとに 1 credit を消費し、Stripe で 3,000、10,000、30,000 credits のパックを購入できます。
- OpenAI 互換および Anthropic Messages の BYOK チャネルを暗号化して保存でき、BYOK では credits を消費しません。
- 手動管理フォルダー以外の文書を対象に、件数に応じて月・週単位を切り替えるスマート整理とアクティビティ整理を追加し、一括移動前に確認できるようにしました。
- 管理画面にホスト全体の CPU、メモリ、負荷、ディスク、稼働時間、ネットワーク帯域を表示するサーバーステータスを追加しました。本番環境では読み取り専用のホスト指標を使用します。
- 新規文書で 15 種類の多言語テンプレートを選べるようにしました。無料版は ToDo リストと汎用テーブルを含むオフライン対応の基本 5 種類、終身会員は日報、週報、OKR、KPI を加えた高度な 10 種類を利用できます。

### Changed

- AI 最適化は送信後にパネルを閉じ、ページ移動や再読み込みをまたいで追跡できるバックグラウンドタスクとして実行します。タイトルと導入、本文セクション、構成・レイアウトを同時実行数付きのサブタスクへ分割し、各段階の進捗と部分結果を保存します。検証失敗時も該当サブタスクだけを再試行します。

### Fixed

- 共有 Stripe アカウントで他のアプリに属する webhook を、異なる Stripe API リリースのイベントも含めて正常に受領・無視し、Koinote 自身の Checkout 検証は引き続き厳密に行うようにしました。
- Word 書き出しを改善し、A4 の文字組みと見出し階層、入れ子リスト、実際のハイパーリンク、引用・コード・画像キャプション・表・ページ番号の統一スタイルを明示しました。
- ブロック画像直後の見出しが Markdown の再保存後も独立して維持され、旧版でエスケープされた見出しは文書を開く際に自動修復されます。
- PDF 書き出しを一つの明確なメニューに統一し、デスクトップではネイティブの印刷ダイアログを開いて選択・検索可能な文字を保持するようにしました。
- デスクトップの権限範囲が AI 設定、モデルチャネル、credits、レビュー、credits 購入を誤って拒否する問題を修正しました。
- Credits 購入を Stripe の本番モード Product に切り替え、テスト／本番モードの不一致をデプロイ前に検出するようにしました。
- Credits 購入でも会員購入と同じ USD、CNY、EUR、JPY を選択でき、CNY では Stripe が WeChat Pay など対象のローカル決済を表示できるようになりました。
- AI 最適化の適用後にデスクトップの revision とリモートスナップショットを整合させ、内容が同期済みでも同期エラーと表示される問題を修正しました。
- デスクトップ画像のアップロード後にローカル参照を画像 URL へ置き換えても、エディターが上へスクロールしないようにしました。
- デスクトップ共有は現在の下書きを先に同期し、共有結果をすぐローカルへ保存するため、有効化や解除が無反応に見える問題を修正しました。

## [0.6.0] - 2026-08-17

### Added

- 多言語のアプリ内お知らせを追加しました。各リリースでユーザー向けの機能改善を自動抽出し、管理者の手動通知はサーバー側の LLM が日本語・英語・中国語・フランス語へ翻訳します。
- アカウント不要の完全ローカルモードを追加しました。パスワードから生成した鍵で文書・フォルダー・画像を暗号化し、すべてのネットワーク通信を遮断します。ログイン後に再度パスワードを確認すると、独立した通常文書としてコピーできます。
- Tauri 2 ベースの macOS / Windows alpha を追加。システムブラウザの PKCE 認証、OS キーチェーン、
  SQLite のローカル優先ドキュメントと画像、遅延アップロード、上限付き画像キャッシュ、同期、競合復旧に対応しました。
- Web サイトから GitHub Releases へ移動でき、macOS Apple Silicon／Intel と Windows x64 の
  インストーラをダウンロードできます。
- クライアントは 6 時間ごとに更新を確認し、一時的な失敗は 30 分後に再試行します。前面復帰時や再接続時にも確認します。
- Web エディターとデスクトップクライアントでリモート更新を検出し、ローカル変更がなければ
  自動反映し、同時編集がある場合は両方を保持して競合解決を促します。
- PostgreSQL を 6 時間ごとに暗号化して非公開 R2 へ退避し、保持、ヘルスチェック、障害通知、復旧手順を追加しました。
- メールアドレスの再確認によるアカウントの即時削除と画像の非同期回収を追加し、必要な決済記録は関連解除後も「削除済みアカウント」として管理画面に残します。
- 本文やトークン内容を記録せず、Agent のツール、トークン、文書、結果、所要時間を確認できる MCP アクティビティログを追加しました。
- 現在版または他の履歴と比較できる行単位 Diff を追加し、大きな文書では処理を制限して未変更部分を折りたたみます。

### Changed

- 利用規約に終身会員、固定 10 GB、将来の AI 利用資格、合理的な利用制限、アカウント削除、返金の境界を明記しました。
- Markdown インポートは大規模な読み込みを検証し、ZIP をバックグラウンド展開して同時送信数を制限します。
  10 MB 超の画像は WebP に圧縮し（大きな GIF は静止画化を通知）、失敗時は未参照の送信画像を解放します。
- macOS 26 はネイティブ Icon Composer アセットを使い、旧 macOS と Windows は従来のフォールバックアイコンを使用します。

### Fixed

- アプリ内お知らせの内容に問題があってもサーバー起動を妨げず、既読保存に失敗した場合もローカルで閉じられるようにしました。管理者は履歴を残したまま取り下げでき、任意の翻訳設定が無関係なデプロイを止めることもありません。
- 文書タイトルで中国語・日本語・韓国語 IME の候補を Enter で確定しても本文へ移動しないよう修正しました。通常の Enter では従来どおり本文へ移動します。
- アカウント削除と Checkout 清理は、ローカルで期限切れになった Session を Stripe で再確認し、完了済みの支払いを webhook 記録まで保持します。ローカルモードの取り込みは上限付きの一時バッチと最終的な原子的コミットを使い、アカウント／版履歴の文言で `$` を含む利用者テキストが変形する問題も修正しました。
- Word／PDF 書き出しの CORS 問題を修正しました。
- Stripe Checkout はユーザーごとに支払い可能な Session を 1 件だけ保持し、安全な `koinote://` コールバックでデスクトップの会員状態を更新します。
- デスクトップの Bearer 許可リストにより管理画面と MCP トークン要求が拒否される問題を修正し、
  ナビゲーションにドキュメントと料金ページを追加しました。Web 限定のアカウント保護操作は、
  403 で失敗させずシステムブラウザで開くようにしました。コピーする招待リンクと共有リンクも、
  Tauri のローカル URL ではなく公開サイトの URL を使用します。
- デスクトップのゴミ箱で、ネイティブ確認を 1 回行うだけで文書を完全削除できるようにしました。
  Web はタイトル入力による保護を維持し、ローカル下書きと未参照のオフライン画像も削除します。
- 画像 1 件の失敗で同期全体が止まる問題を修正し、エラー表示、信頼済みキャッシュ元、未送信画像を含む移行書き出しに対応しました。

## [0.5.0] - 2026-08-15

### Added

- `⌘K` / `Ctrl+K` で開けるタイトル・本文検索、強調表示、MCP 検索スニペットを追加しました。
- Markdown、フォルダー、ZIP のインポートと、文書・画像を含む再インポート可能な一括エクスポートに対応しました。
- 共有ページに OpenGraph、閲覧数、「自分の Koinote にコピー」を追加しました。
- パスワード再設定、変更、セッション失効、モバイル文書ドロワーを追加しました。
- MCP、版履歴、料金、更新履歴の公開ページを追加しました。

### Changed

- MCP トークンは無期限と期限変更に対応し、Codex、Claude Code、OpenCode、OpenClaw、WorkBuddy などの設定例を追加しました。
- WeChat、Zhihu、Juejin への書き出しと、会員向けの版履歴・ゴミ箱・Agent 安全スナップショットを改善しました。
- 管理画面にプライバシーを保護したファネルと D1/D7/D30 継続率を追加しました。

### Fixed

- 共有タイトルや概要の `$` が OpenGraph HTML を壊す問題を修正しました。

## [0.4.0] - 2026-08-13

### Added

- 終身会員向けに、権限付き MCP と revision 競合検出を追加しました。
- 版履歴、復元スナップショット、ゴミ箱、復元フローを追加しました。
- Stripe の支払いを飛書へ安全かつ再試行可能に通知できるようにしました。
- 「マイドキュメント」と「友達を招待」を独立ページにしました。

### Fixed

- Juejin のタイトル、WeChat の画像キャプションとコード装飾、画像参照保護、Cloudflare Analytics を修正しました。

## [0.3.0] - 2026-08-12

### Added

- 10 GB を含む終身会員の多通貨 Stripe Checkout を追加しました。
- 招待した側とされた側に 500 MB、各アカウント最大 5 GB の特典を追加しました。
- ストレージ表示、アップグレード導線、管理統計、Cloudflare Analytics を追加しました。

### Security

- 支払い、OAuth、招待、内部トークン、Checkout 作成の検証を強化しました。

## [0.2.0] - 2026-08-11

### Added

- メール認証コード登録、Cloudflare Email Sending、自動デプロイ、画像容量管理を追加しました。

### Fixed

- 画像回収と容量返却、画像読み込みの再試行、登録失敗時の認証コード処理を修正しました。

### Security

- 認証コードを HMAC とトランザクションで保護し、レート制限を追加しました。

## [0.1.0] - 2026-08-10

### Added

- React、TipTap、Go、PostgreSQL、Cloudflare Worker を使った初回オープンソース版です。
- WYSIWYG Markdown、R2 画像、共有、多形式エクスポート、4 言語 UI、パスワードと OAuth ログインを提供しました。
