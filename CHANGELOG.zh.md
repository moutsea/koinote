# 更新日志

这里记录 Koinote 中值得用户关注的变化。项目遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 新增会员专属 AI 优化：以 Git Diff 展示标题与正文建议，可逐条或全部应用/忽略；标题按 0–100 分评估，低于 60 分时提供 2–3 个候选标题。审阅现已拆分为内容优化与结构排版，并基于 Markdown AST 从层级、可读性、重点、节奏、模块和移动端六个维度给出安全的排版建议。
- 终生会员赠送 1,000 credits；内置模型每 2,000 个实际 token 消耗 1 credit，并支持 $1.99/3,000、$4.99/10,000、$12.99/30,000 三档 Stripe 充值。
- 新增加密保存的 OpenAI 兼容与 Anthropic Messages BYOK 渠道，使用自己的模型不消耗 credits。

### Fixed

- 块级图片后的标题在反复保存后仍保持独立，并在打开文档时自动修复旧版产生的转义标题。
- PDF 导出统一为一个清晰入口，客户端会打开系统原生打印面板并保留可选择、可搜索的文字，不再在内嵌 WebView 中点击无响应。
- 修复客户端 AI 设置、模型渠道、credits、审阅与充值接口被桌面端权限范围错误拒绝的问题。
- Credits 充值已切换到 Stripe 正式模式商品，部署时也会提前拦截测试与正式模式商品混用的问题。

## [0.6.0] - 2026-08-17

### Added

- 新增多语言站内提醒：每次版本发布会自动提取面向用户的功能升级要点，管理员也可手动发布提醒，由后端调用 LLM 生成中文、英文、日文和法文版本。
- 客户端新增完全本地模式：无需账号，密码派生密钥加密本机文档、文件夹与图片，模式内硬阻断所有网络请求；登录账号后可再次验证密码，将本地资料复制为相互隔离的普通账号文档。
- 新增 Tauri 2 macOS / Windows 客户端 alpha：系统浏览器 PKCE 登录、系统钥匙串令牌、
  SQLite 本地优先文档与图片、联网后延迟上传、有限图片缓存、后台同步与显式冲突恢复。
- 官网新增桌面客户端下载入口，GitHub Release 同时提供 macOS Apple 芯片、Intel 与 Windows x64 安装包。
- 客户端每 6 小时自动检查版本更新，临时失败后 30 分钟重试，并在应用回到前台或恢复联网时补检提醒。
- 网页编辑器与桌面客户端新增前台远端更新检测：无本地改动时自动更新，同时编辑时保留双方内容并提示处理冲突。
- 新增每 6 小时一次的 PostgreSQL 异地加密备份，包含私有 R2 保留策略、健康检查、失败告警与恢复演练说明。
- 新增账号自助注销：邮箱二次确认后立即删除账号与内容，异步回收图片；必要财务记录解除账号关联后继续以“已注销账号”显示在管理后台。
- 新增会员可见的 MCP 活动日志，按时间分页展示 Agent 工具、令牌、文档、结果与耗时，不记录正文或令牌内容。
- 版本历史新增逐行 Diff，可与当前版本或其他历史版本比较；大文档使用有界算法并折叠未变化内容。

### Changed

- 服务条款明确终生会员、固定 10 GB、未来 AI 使用资格、合理用量限制、账号注销与退款边界。
- 编辑器侧栏与文档页提供 Markdown 导入入口；大型导入会预检限制、后台解压 ZIP、有限并发上传，
  将超过 10 MB 的图片压缩为 WebP（超大 GIF 会提示转为静态图），失败时回收未引用的已上传图片。
- macOS 26 改用原生 Icon Composer 图标资源，旧版 macOS 与 Windows 继续使用原有回退图标。

### Fixed

- 站内提醒内容异常时不再阻止后端启动；已读状态保存失败时用户仍可本地关闭，管理员可保留历史地撤回提醒，可选翻译配置也不再阻塞无关部署。
- 在文档标题中使用中文、日文或韩文输入法时，按回车确认候选词不再误跳到正文；普通回车仍保留从标题进入正文的快捷交互。
- 账号注销与 Checkout 清理现在会向 Stripe 复核本地过期 Session，已完成的付款会保留到 webhook 入账；本地模式资料改为有限批次暂存并最终原子导入，同时修复账号与版本历史文案中用户文本含 `$` 时被错误替换的问题。
- 修复 Word 与 PDF 导出跨域读取站内图床被 CORS 拦截的问题。
- Stripe 支付只为每位用户保留一个可付款 Session，桌面支付通过安全的 `koinote://` 回跳确认并刷新会员状态。
- 修复客户端管理后台与 MCP 令牌请求被桌面 Bearer 白名单拒绝的问题，在主导航补回“文档”和“价格”，
  并将仅网页允许的账号安全操作转到系统浏览器处理，避免点击后返回 403；客户端复制的邀请链接与
  文档分享链接也会使用公开站点域名，不再生成外部用户无法打开的 Tauri 本地地址。
- 客户端回收站现可在一次系统确认后直接永久删除文档；网页端继续保留输入标题的二次保护，客户端同时清理
  对应本地草稿与不再引用的离线图片，避免后续同步复活。
- 修复单张桌面图片失败会卡住全部同步的问题；错误现在可见，缓存只接受可信来源，迁移导出也会内嵌待上传图片。

## [0.5.0] - 2026-08-15

### Added

- 新增标题与正文全局搜索，支持 `⌘K` / `Ctrl+K`、结果高亮和 MCP 搜索摘要。
- 支持 Markdown、文件夹和 ZIP 导入，并可完整导出文档、目录与图片后再次导入。
- 分享页新增 OpenGraph 卡片、阅读次数和“复制到我的 Koinote”。
- 新增密码找回、修改密码、会话失效和移动端文档抽屉。
- 上线 MCP、版本历史、定价和更新日志公开页面。

### Changed

- MCP 令牌支持永久有效和修改有效期，接入文档覆盖 Codex、Claude Code、OpenCode、OpenClaw、WorkBuddy 等客户端。
- 自媒体导出扩展到微信公众号、知乎和掘金；会员可通过版本历史、回收站和 Agent 安全快照恢复内容。
- 管理后台新增隐私友好的产品漏斗与 D1/D7/D30 留存统计。

### Fixed

- 修复分享标题或摘要包含 `$` 时 OpenGraph 页面结构可能损坏的问题。

## [0.4.0] - 2026-08-13

### Added

- 终生会员可通过带权限范围的 MCP 客户端读写文档，并使用 revision 检测冲突。
- 新增版本历史、恢复快照、回收站与还原流程，保护网页和 Agent 写入。
- Stripe 会员付款可发送持久化、保护隐私的飞书通知。
- “我的文档”和“邀请好友”拆分为独立账户页面。

### Fixed

- 掘金导出补上标题，微信公众号导出保留图注和代码块窗口装饰。
- 历史版本会保护引用图片，上传与文档写入并发时配额仍保持准确。
- Cloudflare Analytics 改用免费套餐可用的数据集。

## [0.3.0] - 2026-08-12

### Added

- 新增多币种 Stripe Checkout：终生会员一次付费获得 10 GB 存储空间。
- 邀请双方各得 500 MB，每个账户的邀请奖励上限为 5 GB。
- 新增存储用量、会员升级入口与管理员统计后台。
- 可选接入 Cloudflare Analytics，查看边缘 UV、PV、请求数和流量。

### Changed

- 存储配额会综合会员等级与邀请奖励计算。
- 微信公式临时图片使用独立限额和内容寻址复用。

### Security

- 支付发放会校验价格、币种、商品、用户和 `metadata.service=koinote`。
- 加固 OAuth 跳转、邀请奖励、内部令牌与 Checkout 创建流程。

## [0.2.0] - 2026-08-11

### Added

- 新增邮箱验证码注册与 Cloudflare Email Sending 发信。
- 新增包含后端和 Worker 健康检查的自动部署。
- 新增按用户统计的图片存储账本与配额。

### Fixed

- 图片回收会复查实时引用，删除后返还配额，短暂加载失败也可自动恢复。
- 账户创建失败时不再消耗验证码。

### Security

- 验证码使用 HMAC 存储和事务消费，并加入限流与请求体大小限制。

## [0.1.0] - 2026-08-10

### Added

- 首个开源版本，采用 React、TipTap、Go、PostgreSQL 与 Cloudflare Worker。
- 支持所见即所得 Markdown、文件夹、标签页、自动保存、代码高亮、表格、任务列表和 KaTeX。
- 支持 R2 图床、受保护分享，以及 Markdown、HTML、DOCX、PDF、打印和微信公众号导出。
- 提供中文、英文、日文、法文界面，并支持密码、Google 与 GitHub 登录。
