import { Link } from "@tanstack/react-router";
import { HardDrive, Plus } from "lucide-react";
import type { AgentWorkspaceStorage } from "../api";
export function AgentWorkspaceStorageCard({ storage, compact = false }: { storage?: AgentWorkspaceStorage; compact?: boolean }) { const used = storage?.usedBytes ?? 0; const quota = storage?.quotaBytes ?? 100_000_000; const percent = Math.min(100, quota ? used / quota * 100 : 0); return <div className={compact ? "rounded-lg border p-4" : "rounded-xl border p-5"} style={{ borderColor: "var(--ink-line)" }}><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-sm font-semibold"><HardDrive className="h-4 w-4" />Agent 空间</span><Link to="/agent/settings" className="inline-flex items-center gap-1 rounded-md border px-2 py-1 text-xs font-semibold"><Plus className="h-3 w-3" />扩容</Link></div><div className="mt-3 h-2 overflow-hidden rounded-full" style={{ background: "var(--ink-wash)" }}><div className="h-full rounded-full" style={{ width: `${percent}%`, background: percent > 90 ? "var(--cinnabar)" : "var(--ink-strong)" }} /></div><p className="mt-2 text-xs" style={{ color: "var(--ink-faint)" }}>{formatBytes(used)} / {formatBytes(quota)}</p></div> }
function formatBytes(bytes: number) { return bytes < 1024 * 1024 ? `${(bytes / 1024).toFixed(1)} KiB` : `${(bytes / (1024 * 1024)).toFixed(1)} MiB`; }
